// Unordered Set --> Stores unique elements in Random order
// TC = O(1), in worst case TC = O(n)
void explainUnorderedSet(){
    unordered set<int> st;
    // Lower_bound and Upper_bound function does not works
    // Rest all functions are same as above
    // It stores elements in random order
    // It has TC better than set in most cases, except some when collision happens
}